The plan is not executable as written. It fails logically before browser integration, and its benchmark can pass without proving the claim.

## Findings

- [P1] `docs/plans/2026-08-29-identitymap-witness.md` §Schedule map — Estimate: 45–88 engineering hours through T12, plus 6–10 human hours. Blowups: T1 3–5h, T8 4–8h, T9 6–12h, T10 10–20h, T11 6–12h, T12 4–8h. T10–T12 alone need roughly 20–40h in an 18h K4 day. Expect K4 to slip 12–30h.

- [P1] Plan T1/T5; `SPEC.md` §§4–6 — The golden fixture cannot produce the asserted witness. Contractors map to `"contractors"`, missing `managerId` evaluates to `null`, and HRIS-first priority satisfies `inv-sot`. Therefore the default state produces no stated violations, while the oracle says P3—whose expected manager value is explicitly `null`—witnesses `inv-null`. T5 cannot turn green honestly.

- [P1] Plan T1/T3/T12; `EVAL.md` Benchmark D1 — `direction` is passed to `evaluate` but has no semantics; expressions are not keyed by direction, and no fixture state represents a wrong-direction row. The 8×2-direction oracle and D1 recall are undefined.

- [P1] Plan T3; `EVAL.md` Layer 1 provenance — EVAL requires the losing source to be named, and the UI requires a source chain. T3’s provenance contains only the selected source and selected inputs. That test/UI contract cannot be implemented from the stated interface.

- [P1] Plan T6/T8; `SPEC.md` §§2,4 — “Precise closure” is false. `PIN_INVARIANTS` carries a list while invalidation expects one `id`; adding a pin does not stale old packets; editing a field previously proven clean can create a violation without staling evidence; dependencies include only violated paths. Use a complete query fingerprint or dependencies for every checked pin/path.

- [P1] Plan T6/T8/T11 — Revision semantics conflict. SPEC says every mutation bumps revision, but T11 assumes recording evidence does not bump it. The store also exposes no packet-recording method although `prepare_mapping_review` must create packets.

- [P1] Plan T8; `SPEC.md` §7 — Four tool contracts are materially incomplete: exact input schemas, output shapes, staging replacement/append behavior, patch-preview behavior, evidence granularity, and packet-green rules are absent. “Per SPEC §7” cannot resolve them.

- [P1] Plan T8–T11 — T9 imports nonexistent `src/tools/defs-browser.js`; T8 creates only `defs.mjs` and `run.mjs`. T11 then tries to dispatch through `Runtime.evaluate`, but T9 exposes neither the store nor a DOM automation contract. Layer 2 cannot load, and Layer 3 cannot perform its human edit.

- [P1] Plan T9/T13; `SPEC.md` C9/C10 — Registration omits C10’s second `{signal}` argument. Render cannot publish “`src/page` + `src/` + `data/`” as one static directory, so remote imports/data fetches lack a defined deployment layout. K5 is exposed to predictable 404s.

- [P1] Plan T12; `EVAL.md` arm B/K1 — Arm B is engineered to lose: it receives the same engine but is deliberately stripped of exactly the dirty expressions and pins needed for recall. T12 even directs Ralph to “fix” the snapshot until the expected loss appears. This is a persisted-state ablation, not an honest API baseline; K1 is non-falsifiable.

- [P1] Plan T1/T11/T12; `EVAL.md` Benchmark/Thresholds — No machine-readable scorer maps five defect classes to oracle cases and relay observations. The C trace does not exercise five classes or both directions. The same agent authors fixture, oracle, implementation, snapshot, and scorer; `audited:false` is never changed by an automated task. Green demonstrates self-consistency only.

- [P1] `EVAL.md` Benchmark — Arm A is a formula, not an observed native workflow. Browser-Use and Full-CDP have zero evidentiary value because they are not run. Arm C is a scripted CDP client that already knows every tool and argument; it is protocol E2E, not agent or ChatGPT E2E. Calling this a three-arm benchmark will be attacked.

- [P1] `EVAL.md` Layers 1–3 — Demo-blocking missing tests: both directions; clean-to-violating closure invalidation; pin addition/removal against existing packets; final serialized success and error responses including canaries in keys/arguments; production-directory loading; repeated cold Chrome sessions with cleanup; and remote stale/revision recovery.

- [P1] `EVAL.md` K1–K5; `RALPH.md` Abort semantics — K1 is rigged; K2/K3’s “survives a same-day fix” is not executable; K4 ignores Layer 3 and deployment readiness; K5 merely says “demo runs,” not that the stale/recovery flow runs. ABORT is not the configured completion promise, so Ralph continues until cancellation or max iterations.

- [P1] Plan T3/T13; `RALPH.md` execution/completion — T3 implementation and commit are already checked despite no code. The loop skips implementation and wedges on green. Its commit instructions also conflict over whether intentionally failing-test steps must be committed. T13 completion is circular: checking its final box requires all boxes—including itself—to be checked.

- [P1] Plan T13; `RALPH.md` Completion — Local commands can emit `IDENTITYMAP COMPLETE` while deployment, remote ChatGPT evidence, oracle audit, video, public repo, and submission remain unfinished. “Code complete” and “entry ready” are conflated.

- [P2] Plan T5 — Greedy set cover is not generally minimal. It happens to be safe for a universe capped at three invariants, but exhaustive search across eight personas is simpler and defensible.

- [P2] Plan T1/T7 — The fixture test claims every identity value is a canary but checks only two Okta properties. It misses last names and HRIS/AD/app identity fields.

- [P2] `EVAL.md` Thresholds — Five classes have 20-point granularity; K1’s 5% margin is pseudo-precision. A’s interaction threshold and zero wrong-direction writes are effectively guaranteed by construction.

- [P1] `SPEC.md` §2 — Strongest rebuttal: “Nothing here demonstrates WebMCP leverage: give any page-local API or DOM agent the same dirty draft and pins and it runs the identical engine; your API arm loses only because you withheld those inputs.”

## VERDICT

No-go as planned. Ralph will likely wedge at T3 or T5; if manually pushed through, T9/T10 and deployment become the next failures. A green report would still be self-confirming rather than credible evidence.

## WIN PROBABILITY

**As-is: 3% / With fixes: 12%**

## TOP 3 FIXES

1. Freeze a coherent golden contract before coding: exact dirty initial state, two-direction semantics, tool schemas, revision rules, packet criteria, and complete closure dependencies. Prove the expected witness manually.

2. Reorder around one deployed vertical slice and replace Ralph’s checkbox predicate with an external verifier. Separate `CODE_COMPLETE` from timestamped `ENTRY_READY`; reset T3; make abort gates executable.

3. Repair the evaluation: use a frozen audited holdout, relabel B as a persisted-state ablation or give it equal inputs, delete simulated/designed arms from headline claims, and run repeated cold local plus remote ChatGPT flows.
